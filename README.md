
# lpkg (Leaf Package Manager)

The Leaf Package Manager, is a binary package manager for unix-like systems,
written for POSIX C99.

## Dependencies

- inih
- libarchive
- sqlite3

    meson setup build
    ninja -C build
    meson -C build install

