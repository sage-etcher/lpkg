
#ifndef LPKG_VERSION
#define LPKG_VERSION

/* if x >  y returns positive
 * if x <  y returns negative
 * if x == y returns 0 */
int version_cmp (const char *x, const char *y);

#endif
/* end of file */
